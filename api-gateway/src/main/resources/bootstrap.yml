spring:
  application:
    name: api-gateway-server
  cloud:
    config:
      discovery:
        enabled: true

management:
  security:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/