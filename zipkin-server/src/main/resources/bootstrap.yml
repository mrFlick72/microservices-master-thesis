spring:
  application:
    name: zipkin-server
  cloud:
    config:
      discovery:
        enabled: true
  zipkin:
    locator:
      discovery:
        enabled: true

management:
  security:
    enabled: false