server:
  port: 8888

management:
  security:
    enabled: false

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://home/vvaudi/Desktop/microservices-master-thesis-configurations
