server:
  port: 8888

management:
  security:
    enabled: false

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mrFlick72/microservices-master-thesis-configurations.git